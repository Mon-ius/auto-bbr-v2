{
  "name": "Ubuntu-Z",
	"build": {
		"dockerfile": "Dockerfile"
	},
  "settings": {},
  "runArgs": [
    "--net=host",
    "--cap-add=NET_ADMIN",
    "--cap-add=SYS_ADMIN",
    "--device=/dev/net/tun",
    "--restart unless-stopped"],
  "workspaceFolder": "/home/monius",
  "remoteUser": "monius"
}